venv/
__pycache__/
*.py[cod]
.pytest_cache/
.hypothesis/
.cursor/
*.log

# 运行期生成（可不提交）
logs/
hr_excel_web/uploads/
hr_excel_web/exports/
hr_excel_web/audit_log.csv
# 用户自助修改后的口令哈希（运行期生成）
hr_excel_web/password_overrides.json

# 发布包
CASE-Excel_merge_release_*.tar.gz
CASE-Excel_merge_release_*.zip

# Office 打开文件时的临时锁定文件（勿提交）
~$*.xlsx
~$*.xls
~$*.xlsm

# 其它项目导出目录（运行期生成）
intranet_employee_chat_project/exports/

# 离线 wheel 体积大：在 Linux（与 ECS 同版本）执行 scripts/download_offline_wheels.sh 生成后打入发布包，一般不提交仓库
offline_wheels/*.whl
offline_wheels/*.tar.gz
offline_wheels/*.zip
offline_wheels/.requirements.sha256
